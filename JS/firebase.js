/* =========================================================
   FIREBASE.JS
========================================================= */

const firebase = window.firebase;
const db = window.db;

export {
  firebase,
  db
};